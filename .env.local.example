# ============================================================================
# MeshCore to MQTT - Example Local Configuration
# ============================================================================
# Copy this file to .env.local and customize for your setup
# cp .env.local.example .env.local
# ============================================================================

# -----------------------------------------------------------------------------
# Serial Configuration
# -----------------------------------------------------------------------------
SERIAL_PORTS=/dev/ttyACM0
# Multiple ports: SERIAL_PORTS=/dev/ttyACM0,/dev/ttyACM1

# -----------------------------------------------------------------------------
# Location Configuration
# -----------------------------------------------------------------------------
# 3-letter IATA airport code for your geographic location
# Examples: SEA (Seattle), LAX (Los Angeles), NYC (New York), LON (London)
IATA=SEA

# -----------------------------------------------------------------------------
# MQTT Broker 1 - Example: Local MQTT with Username/Password
# -----------------------------------------------------------------------------
MQTT1_ENABLED=true
MQTT1_SERVER=mqtt.local.example.com
MQTT1_PORT=1883
MQTT1_USERNAME=your_username
MQTT1_PASSWORD=your_password

# -----------------------------------------------------------------------------
# MQTT Broker 2 - Example: Public Observer Network with Auth Token
# -----------------------------------------------------------------------------
# Learn more: https://analyzer.letsme.sh/observer/onboard
MQTT2_ENABLED=false
MQTT2_SERVER=mqtt-us-v1.letsmesh.net
MQTT2_PORT=443
MQTT2_TRANSPORT=websockets
MQTT2_USE_TLS=true
MQTT2_USE_AUTH_TOKEN=true
MQTT2_TOKEN_AUDIENCE=mqtt-us-v1.letsmesh.net

# -----------------------------------------------------------------------------
# Advanced: Per-Broker Topic Overrides (Optional)
# -----------------------------------------------------------------------------
# Override topics for specific brokers
# MQTT2_TOPIC_STATUS=custom/{IATA}/{PUBLIC_KEY}/status
# MQTT2_TOPIC_PACKETS=custom/{IATA}/{PUBLIC_KEY}/packets
# MQTT2_IATA=LAX

# -----------------------------------------------------------------------------
# Advanced: TLS Configuration (Optional)
# -----------------------------------------------------------------------------
# For brokers with TLS but invalid/self-signed certificates
# MQTT1_USE_TLS=true
# MQTT1_TLS_VERIFY=false  # WARNING: Disables certificate verification (insecure)

