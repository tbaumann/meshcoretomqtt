# ============================================================================
# MeshCore to MQTT - Default Configuration
# ============================================================================
# DO NOT EDIT THIS FILE - Create .env.local to override settings
# This file contains default values and will be overwritten on updates
# ============================================================================

# -----------------------------------------------------------------------------
# Update Configuration
# -----------------------------------------------------------------------------
# Source repository and branch for updates (configured by installer)
UPDATE_REPO=michaelhart/meshcoretomqtt
UPDATE_BRANCH=main

# -----------------------------------------------------------------------------
# Serial Configuration
# -----------------------------------------------------------------------------
SERIAL_PORTS=/dev/ttyACM0
SERIAL_BAUD_RATE=115200
SERIAL_TIMEOUT=2

# -----------------------------------------------------------------------------
# Topic Configuration
# -----------------------------------------------------------------------------
# Template variables supported: {IATA}, {PUBLIC_KEY}
# Example: meshcore/{IATA}/{PUBLIC_KEY}/status
TOPIC_STATUS=meshcore/{IATA}/{PUBLIC_KEY}/status
TOPIC_PACKETS=meshcore/{IATA}/{PUBLIC_KEY}/packets
TOPIC_DEBUG=meshcore/{IATA}/{PUBLIC_KEY}/debug
TOPIC_RAW=meshcore/{IATA}/{PUBLIC_KEY}/raw

# Global IATA code (used if not specified per broker)
# This is a 3-letter airport code identifying your geographic region
IATA=XXX

# -----------------------------------------------------------------------------
# MQTT Broker 1 Configuration
# -----------------------------------------------------------------------------
MQTT1_ENABLED=false
MQTT1_SERVER=mqtt.example.com
MQTT1_PORT=1883
MQTT1_TRANSPORT=tcp
MQTT1_USE_TLS=false
MQTT1_TLS_VERIFY=true
MQTT1_CLIENT_ID_PREFIX=meshcore_
MQTT1_QOS=0
MQTT1_RETAIN=true
MQTT1_KEEPALIVE=60

# Authentication - Username/Password
MQTT1_USERNAME=
MQTT1_PASSWORD=

# Authentication - Auth Token (alternative to username/password)
MQTT1_USE_AUTH_TOKEN=false
MQTT1_TOKEN_AUDIENCE=

# Per-broker topic overrides (optional)
# MQTT1_TOPIC_STATUS=meshcore/{IATA}/{PUBLIC_KEY}/status
# MQTT1_TOPIC_PACKETS=meshcore/{IATA}/{PUBLIC_KEY}/packets
# MQTT1_IATA=XXX

# -----------------------------------------------------------------------------
# MQTT Broker 2 Configuration (Optional)
# -----------------------------------------------------------------------------
MQTT2_ENABLED=false
# MQTT2_SERVER=mqtt-us-v1.letsmesh.net
# MQTT2_PORT=443
# MQTT2_TRANSPORT=websockets
# MQTT2_USE_TLS=true
# MQTT2_USE_AUTH_TOKEN=true
# MQTT2_TOKEN_AUDIENCE=mqtt-us-v1.letsmesh.net

# -----------------------------------------------------------------------------
# MQTT Broker 3 Configuration (Optional)
# -----------------------------------------------------------------------------
MQTT3_ENABLED=false
# Add MQTT3_* configuration as needed

# -----------------------------------------------------------------------------
# MQTT Broker 4 Configuration (Optional)
# -----------------------------------------------------------------------------
MQTT4_ENABLED=false
# Add MQTT4_* configuration as needed

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

